# 任务指令：HTML电子教材轻量级防篡改 + 身份指纹系统改造
## 一、任务背景
你需要对一份**超过5000行代码的中英双语互动HTML电子教材**进行轻量级代码改造，该教材将免费发放给学生用于学习。改造的核心目标是：提高普通学生随意修改教材内容的门槛，同时增加强制身份记录功能（电子指纹）实现使用溯源；全程不限制学生访问权限，仅做内容保护与身份标识，不破坏原有任何教学功能。

## 二、核心约束（必须严格遵守）
1. **绝对非破坏性**：不得修改、删除原有教材的任何文本内容、交互逻辑、排版样式、双语切换功能；所有新增保护逻辑必须与原有业务代码解耦，不能影响原有功能运行。
2. **极致轻量**：所有新增代码总体积控制在20KB以内，不能降低页面加载速度、不能造成交互卡顿，兼容Chrome、Edge、Firefox、Safari等主流桌面浏览器+常见的安卓/苹果手机的浏览器。
3. **可维护性**：所有新增代码必须标注清晰的功能分区与起止标识，方便后续教材内容更新时维护。

## 三、模块一：轻量级防篡改体系实现
### 目标：提升普通学生修改教材的技术门槛，检测篡改行为并主动提示，不做强制加密
1. **核心保护代码混淆**
   - 仅对「防篡改逻辑、身份验证逻辑」的JavaScript代码进行混淆+压缩处理，提高代码阅读和修改的难度；原有教材的业务代码、课文文本完全不做混淆，避免影响原有交互。
   - 混淆后的保护代码统一嵌入HTML文件`</body>`标签前，用`/* ========== 保护逻辑开始 ========== */`和`/* ========== 保护逻辑结束 ========== */`做明确标注。

2. **内容完整性校验**
   - 自动识别教材核心内容区域（课文正文、练习题、交互模块的核心配置节点），页面初始化时生成DOM内容哈希校验值。
   - 通过`MutationObserver`持续监听核心内容节点的所有变化（文本修改、节点增删、关键属性修改），一旦检测到内容与初始校验值不符，立即弹出常驻提示：「警告：本教材内容已被篡改，可能影响学习效果，请使用官方原版文件」。
   - 篡改提示仅作警示，不强制恢复内容，避免误判影响学生正常使用。

3. **基础防操作限制**
   - 禁用页面右键菜单，屏蔽「查看网页源代码、检查」等选项；**保留文本选中、复制功能**，不影响学生正常学习摘抄。
   - 屏蔽常见开发者工具快捷键：F12、Ctrl+Shift+I、Ctrl+Shift+J、Ctrl+U、Ctrl+S，降低普通学生查看/修改源码的可能性。
   - 检测到开发者工具被打开时，触发页面内容轻度模糊提示，不强制锁死页面。

4. **样式结构防护**
   - 对核心内容区域的关键样式做JS动态校验，防止通过CSS隐藏、覆盖核心教学内容；检测到样式被篡改时触发篡改提示。

## 四、模块二：强制用户身份电子指纹系统
### 目标：打开HTML必须输入用户名，不可绕开，生成唯一电子指纹并留存，实现使用溯源
1. **不可绕过的身份验证流程**
   - 页面加载时优先渲染全屏半透明遮罩层，完全覆盖所有页面内容；遮罩层仅包含「姓名输入框+确认按钮+提示文字」，提示语为：「请输入您的姓名完成身份验证，身份信息仅用于学习溯源，不会上传至任何服务器」。
   - 验证逻辑与内容渲染强绑定：未通过验证时，教材核心内容DOM默认不生成、不显示；若通过控制台强行删除遮罩层，会触发核心内容自动隐藏，无法查看教材。
   - 输入校验：姓名不能为空、不能为纯空格，长度限制2-20字符，校验通过后才销毁遮罩、完整渲染教材内容。

2. **电子指纹生成与留存**
   - 指纹组成：用户输入的姓名 + 首次访问时间戳 + 浏览器环境指纹（由userAgent、屏幕分辨率、浏览器语言、系统版本等前端可获取信息生成短哈希标识）。
   - 留存方式：
     ① 双重本地存储：加密后的指纹信息同时存入`localStorage`和`sessionStorage`，降低学生轻易删除本地记录的概率。
     ② DOM嵌入留存：验证通过后，将「姓名+指纹短标识」以半透明浅灰色小字的形式，动态嵌入页面页脚右下角（不遮挡教学内容），同时写入页面DOM注释中；若学生将文件另存为本地HTML，该身份信息会保留在文件内，实现篡改溯源。
   - IP补充方案：纯前端HTML无法直接获取客户端真实IP，可集成免费第三方公开接口（如ipify.org）补充公网IP到指纹信息中；必须做好降级处理，接口不可用时自动跳过IP获取，不影响正常使用。

3. **二次访问逻辑**
   - 同一浏览器再次打开教材时，自动读取本地存储的指纹信息，无需重复输入姓名，直接进入教材。
   - 页脚的身份标识支持点击修改，修改时需要二次确认，避免误操作。

4. **禁用JS容错**
   - 若浏览器禁用JavaScript，页面仅显示提示文字：「请启用JavaScript以正常使用本电子教材」，不展示任何教材内容。

## 五、界面优化
   Read https://lobehub.com/skills/affaan-m-everything-claude-code-frontend-design/skill.md to understand how to install and use this skill. Install it as documented, then open the installed SKILL.md and follow its instructions. Ask for confirmation before any credential use, external service setup, or irreversible action.
1. 安装 Frontend-Design skill，保存进度，重启CC
2. 调用/front-end design skill, 美化一下此网页的视觉设计，让页面更适合学生阅读，采用复古印刷设计风格，不要影响原有的网页交互设计

## 六、局限性说明（交付时必须明确标注）
请在交付内容中清晰、完整地说明以下技术边界：
1. 纯前端HTML方案无法实现绝对防篡改，仅能提高普通用户的修改门槛，无法阻止具备前端技术的人员深度修改代码。
2. 本地存储的指纹信息可通过清除浏览器数据删除，嵌入DOM的指纹可通过手动编辑HTML移除，仅作为常规溯源手段。
3. IP获取依赖第三方公共接口，存在接口不可用、获取不准确的可能。

## 七、最终交付要求
1. 输出修改后的完整HTML文件代码，原有所有内容、格式、交互完全保留，新增保护逻辑无报错、无样式错乱。
2. 在代码末尾的`<!-- 改造说明 -->`注释块中，清晰列出：新增功能清单、防篡改措施说明、指纹存储与溯源方法、上述局限性说明。
3. 确保所有新增逻辑不与原有教材的交互代码产生冲突，双语切换、习题交互等原有功能完全正常。